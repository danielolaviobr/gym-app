import { PrismaClient } from "@prisma/client";
import { JWT_VAID_FOR_DAYS } from "constants/auth";
import { addDays } from "date-fns";

export default function registerMiddlewares(prisma: PrismaClient) {}
